trigger createCase_Account on Account (after insert) {
    /*
    for(Account acc : Trigger.new){
        Case myCase = new Case();
        myCase.OwnerId = '0051U000002NQns';
        myCase.Subject = 'Dedupe this account';
        myCase.AccountId = acc.Id;
        insert myCase;
    }
*/
}